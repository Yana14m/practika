"""
ГЛАВНЫЙ ФАЙЛ ЗАПУСКА БОТА
=========================

При запуске бота:
1. Создаются таблицы в базе данных (если их нет)
2. Запускается планировщик уведомлений (фоновый режим)
3. Запускается сам бот (обработка сообщений от пользователей)
"""

# ============================================================================
# ИМПОРТЫ
# ============================================================================

from database import init_db  # Функция создания таблиц 
from notifications import start_scheduler  # Запуск планировщика 
from bot import run_bot  # Запуск бота 


# ============================================================================
# ТОЧКА ВХОДА
# ============================================================================

if __name__ == '__main__':
    """
    ПОРЯДОК ЗАПУСКА:
    1. init_db() — создаём таблицы в базе данных (если их нет)
    2. start_scheduler() — запускаем фоновый планировщик уведомлений
    3. run_bot() — запускаем бота (он принимает и обрабатывает сообщения)
    
    ВАЖНО: Планировщик работает в отдельном потоке, поэтому не блокирует бота.
    Бот и уведомления работают параллельно.
    """
    
    print("=" * 60)
    print("ЗАПУСК MAX-БОТА")
    print("=" * 60)
    
    # ШАГ 1: Инициализация базы данных
    print("?? Инициализация базы данных...")
    init_db()
    print("   ? База данных готова")
    
    # ШАГ 2: Запуск планировщика уведомлений
    print("? Запуск планировщика уведомлений...")
    start_scheduler()
    print("   ? Планировщик запущен")
    
    # ШАГ 3: Запуск бота
    print("?? Запуск бота...")
    print("-" * 60)
    run_bot()  # Эта функция не возвращает управление (бот работает постоянно)
    
    # Сюда мы не попадём, пока бот не остановится
    print("?? Бот остановлен")