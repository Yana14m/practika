[project]
name = "guap-deadline-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "obabot",
    "umaxbot>=0.1.7",
    "apscheduler>=3.10",
    "beautifulsoup4>=4.12",
    "pydantic-settings>=2.0",
    "requests>=2.31",
]

[tool.uv]
package = false
